kind: Service
apiVersion: v1
metadata:
  name: mg-ambulance-webapi
spec:
  selector:
    pod: mg-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port